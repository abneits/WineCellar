# URL of the already-running WineCellar app
APP_URL=http://localhost:8080

# Separate PostgreSQL test database (migrations must be applied)
# NEVER point this at the production database
TEST_DATABASE_URL=postgresql://user:password@host:5432/winecellar_test
